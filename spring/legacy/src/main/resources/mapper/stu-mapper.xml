<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 해당 파일에 모든 쿼리문을 작성 -->
<!-- namespace에는 xml파일과 연결할 Mapper interface 파일명을 패키지를 포함하여 적어주세요 -->
<mapper namespace="com.green.legacy.stu_manage.mapper.StuMapper">
    <resultMap id="cls" type="com.green.legacy.stu_manage.dto.ClassDTO">
        <id     property="classNum" column="CLASS_NUM"/>
        <result property="className"    column="CLASS_NAME"/>
    </resultMap>
    <resultMap id="stu" type="com.green.legacy.stu_manage.dto.StuDTO">
        <id     property="stuNum" column="STU_NUM"/>
        <result property="stuName"    column="STU_NAME"/>
        <result property="stuYear"    column="STU_YEAR"/>
        <result property="classNum"    column="CLASS_NUM"/>
        <result property="className"    column="CLASS_NAME"/>
    </resultMap>

    <select id="selectClassList" resultMap="cls">
        SELECT CLASS_NUM
        , CLASS_NAME
        FROM ASYNC_CLASS
    </select>

    <select id="selectStuList" resultMap="stu">
        SELECT S.STU_NUM,
            S.STU_NAME,
            S.STU_YEAR,
            S.CLASS_NUM,
            C.CLASS_NAME
        FROM ASYNC_STUDENT S
        JOIN ASYNC_CLASS C ON S.CLASS_NUM = C.CLASS_NUM
        <where>
            <if test="classNum != 0">
                S.CLASS_NUM = #{classNum}
            </if>
        </where>
        ORDER BY S.STU_NUM
    </select>

</mapper>































